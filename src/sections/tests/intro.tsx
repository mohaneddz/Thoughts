export function TestsIntro() {
  return (
    <section className='space-y-3'>
      <p className='text-sm font-semibold uppercase tracking-[0.18em] text-[var(--color-muted)]'>Find the right entry point</p>
      <h1 className='font-heading text-5xl text-[var(--color-text-strong)]'>Test Library</h1>
      <p className='max-w-2xl text-[var(--color-muted)]'>
        Start with something small, not necessarily something clinical. The goal is a clearer pattern, not a harder experience.
      </p>
    </section>
  );
}

